<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/nc_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/nc_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".final_information"></activity>
        <activity android:name=".jackets"></activity>
        <activity android:name=".t_shirts" />
        <activity android:name=".pants"></activity>
        <activity android:name=".reaching" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>